{% extends 'base.html.twig' %}

    {% block header %}{% endblock %}
    {% block footer %}{% endblock %}

{% block body %}
        <h1>Edit user</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
            <input type="submit" value="{{ button_label|default("Create user") }}"
                class="{{ button_css|default("btn btn-primary") }}" />
        {{ form_end(form) }}

{% endblock %}
